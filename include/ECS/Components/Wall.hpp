/*
** EPITECH PROJECT, 2024
** server
** File description:
** Text
*/

#ifndef WALL_HPP_
#define WALL_HPP_

#include <SFML/Graphics.hpp>
#include <iostream>
#include <memory>
#include <string>

/**
 * @struct Wall
 * @brief Structure representing text in the user interface.
 *
 * This structure is used to define a text object in the game’s user interface (UI).
 * It includes properties such as the font used to render the text, the text itself, the position
 * of the text on the screen, and the size of the text.
 */
struct Wall {};

#endif /* !WALL_HPP_ */
